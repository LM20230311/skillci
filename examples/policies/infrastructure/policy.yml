# Infrastructure Skills require a deliberately narrow review boundary.
allow:
  network:
    - api.github.com
deny:
  paths:
    - .env
    - **/*.tfstate
  commands:
    - kubectl delete
  commandPatterns:
    - terraform apply *-auto-approve
  workingDirectories:
    - infra/prod/**
